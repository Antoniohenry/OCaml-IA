(*module analyser*)

(*prend en parametre une grille vierge et verifie si on peut reellement l'utiliser en tant que grille de mot croises, retourne la meme si pas possible, retourne la premiere solution sipossible *)

(*analyse horizontale et verticale de la grille/ Algorithme de BackTrack *)
