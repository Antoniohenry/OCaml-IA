(*module display *)

(*affiche une fenetre avec la grille d'origine*) 

(* premiere version non graphique *)

(*let display = fun crossword ->
        for i = 0 to Array.lenght crossword -1 do
                let line = "" in
                for j = 0 to Array.lenght crossword.(0) -1 do
line 
*)
